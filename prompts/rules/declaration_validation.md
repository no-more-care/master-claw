Classify whether the declared action is possible from the supplied actor's character sheet and scene facts, and choose its resolution path: `roll`, `automatic`, or `clarification`. This classification does not execute the action. A player may declare only their own character's action; never accept a declaration that controls another player character. Never borrow another participant's traits, aspects, flags, or equipment unless deterministic context explicitly says that participant is helping and makes the resource available. Do not invent equipment, abilities, aspects, permissions, or unseen facts. Cite concrete supplied facts in `evidence` for `roll` and `automatic`. If essential information is absent, request clarification. Never resolve the outcome, roll dice, or narrate consequences.
