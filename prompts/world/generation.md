Generate only the requested world section from the outline and constraints. Mark new identifiers explicitly, preserve existing identifiers, and do not silently rewrite other sections. Treat secret plot material as non-public.

